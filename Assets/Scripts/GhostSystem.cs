using UnityEngine;

public class GhostSystem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // TODO: Add replay recorder
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
